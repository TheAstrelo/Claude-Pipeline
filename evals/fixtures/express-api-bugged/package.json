{
  "name": "express-api-bugged-fixture",
  "version": "1.4.2",
  "description": "A tiny Express API with a seeded DELETE bug, used as an evaluation fixture for the pipeline",
  "main": "src/index.js",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test \"src/**/*.test.js\""
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
