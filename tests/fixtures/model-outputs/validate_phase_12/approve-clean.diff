diff --git a/src/routes/auth.js b/src/routes/auth.js
index 3b18e51..a4c2f10 100644
--- a/src/routes/auth.js
+++ b/src/routes/auth.js
@@ -36,8 +36,12 @@ router.post('/login', async (req, res) => {
   const user = await users.findByEmail(req.body.email);
-  if (!user) return res.status(404).json({ error: 'no such user' });
+  if (!user) {
+    return res.status(401).json({ error: 'invalid credentials' });
+  }
+  const ok = await bcrypt.compare(req.body.password, user.passwordHash);
+  const token = signSession(user.id);
+  res.json({ token });
 });
diff --git a/src/acceptance/auth.test.js b/src/acceptance/auth.test.js
new file mode 100644
index 0000000..7c1d2e3
--- /dev/null
+++ b/src/acceptance/auth.test.js
@@ -0,0 +1,9 @@
+const request = require('supertest');
+const { app } = require('../app');
+
+test('POST /login returns a token for valid credentials', async () => {
+  const res = await request(app).post('/login').send({ email: 'a@b.c', password: 'hunter2' });
+  expect(res.status).toBe(200);
+  expect(res.body.token).toBeDefined();
+});
